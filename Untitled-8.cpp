#include<iostream>
#include<vector>

using namespace std;

void merge(vector<int>v,vector<int>v1,vector<int>v2)
{
    int m;

    for(int i=0;i<m;i++)
    {
        
    }

}

    void div(vector<int> v)
    {
        int m;//v.size 
        int p = m/2; // v1.size()
        int q = m - m/2;  // v2.size()

        vector<int>v1,vector<int>v2;
            for(int i=0;i<p;i++)
            {
                v1[i] == v[i];
            }

            for(int i=p;i<m;i++)
            {
                v2[i] == v[i];
            }   
    }
int main()
{
    int n;
    vector<int> v;
    
     for(int i=0;i<n;i++)
     {
        int digit;
        cin>>digit;
        v.push_back(digit);
     }

     div(v);


}